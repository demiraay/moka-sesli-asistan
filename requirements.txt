# Moka Sesli Asistan - Python bagimliliklari
# Kurulum: pip install -r requirements.txt

# Web katmani (admin paneli + call API + WhatsApp koprusu)
Flask>=3.0
python-dotenv>=1.0

# Testler
pytest>=8.0

# OPSIYONEL - lokal STT fallback (GROQ_API_KEY yoksa kullanilir; ffmpeg gerekir):
#   pip install openai-whisper
